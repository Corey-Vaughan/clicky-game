import React from "react";
// import "./Jumbotron.css";

const Jumbotron = props => (
  <div className="jumbotron jumbotron-fluid bg-cover text-white text-center">
    <h1 className="display-4 font-weight-bold">Overwatch Clicky Game</h1>
    <p className="lead font-weight-bold">Click images to earn points, but don't click the same one twice</p>
  </div>
);

export default Jumbotron;